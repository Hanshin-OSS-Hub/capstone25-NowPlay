다음으로 “Figma를 변경할 수 없을 때”의 구체 작업 계획을 TODO에 반영하겠고, 추천 워크플로우와 선택지를 제안하겠습니다.

Starting: Choose conversion method (2/7)

요약(짧게)

상황: Figma는 수정 불가 → Figma에서 내보낸 React 코드를 그대로 가져와서 로컬에서 정리(post-process)해야 합니다.
권장 방식: 자동 변환 코드 그대로 붙여넣고, 프로젝트 내에서 “포스트 프로세싱(정리·컴포넌트화·스타일 추출)”을 수행하세요.
무엇을, 왜, 어떻게 할지 (실행 가능한 체크리스트)

에셋/토큰 수집 (자동화 가능)
Figma에서 받은 export 폴더(icons, images)를 assets 또는 public로 옮기세요.
사용한 폰트(예: Pretendard, Nunito) 정보를 확인해 프로젝트에 설치(또는 Google Fonts link)하세요.
색상/간격을 수동으로 기록하거나 가능하면 플러그인이 제공한 JSON을 확보하세요.
JSX/스타일 문제 자동/수동 수정 (필수)
style 속성이 문자열로 되어 있으면 객체로 변경 (예: style="color: '#123'" → style={{color: '#123'}}) — 이미 일부 자동으로 고친 작업을 했습니다.
inline style 중 잘못된 속성(예: outline: '3px #7A...'는 React에서는 문자열 OK지만 일부 CSS 값은 단위 필요) 검토.
백분율/픽셀/숫자 표기 일관화(예: width: '412' → width: 412 또는 '412px' 규칙 통일).
레이아웃(절대위치 → 컨테이너) 조정(권장)
앱 수준에서 모바일 프레임(width: 412px)에 페이지를 렌더하고 있기에, 각 페이지의 절대 좌표(left/top)와 position:absolute를 유지하더라도 프레임 안에서 Figma와 비슷해 보입니다.
더 정확한 일치와 유지보수를 위해 절대 배치 요소를 가능한 한 Flexbox/Auto Layout(=CSS Flex)로 재구성하세요. (자동 툴로 완벽 변환되지 않음 → 수동 리팩토링 필요)
스타일 추출 및 토큰화(중요)
반복되는 inline style들을 CSS 클래스(.page, .card, .btn 등)로 추출하세요.
:root 또는 CSS 변수로 색상/폰트/간격 토큰 생성 → 전체 페이지에 적용하면 쉽게 조정 가능.
컴포넌트화
버튼, 카드, 네비바, 리스트 아이템 등 반복 요소를 React 컴포넌트로 분리하세요. (디자인 변경이 필요할 때 한 곳만 수정하면 됩니다)
이미지/비디오 플레이어 처리
피그마는 플레이어 '프레임'만 export합니다. 실제로 동작하는 플레이어(예: HTML5 <video> 또는 오디오 플레이어 컴포넌트)를 직접 연결해야 합니다.
테스트 & 반복
브라우저에서 비교하며 margin, font-size, line-height 등을 tweak하세요.
모바일 크기(412px)로 확인 — App.jsx에 이미 모바일 프레임을 만들어 두었으니 활용하세요.
구체적 추천 전략 (당장 적용 가능)

빠른 방법: “내보낸 코드 → assets/ 폴더 → App.jsx에 모바일 프레임” 그대로 사용하고, 아래 세 가지만 수동으로 고치면 시각적으로 거의 동일해짐

최상단 루트 div를 height:auto; min-height:100vh로 변경(이미 적용).
모든 style="..." (문자열) → style={{...}}로 바꾸기 (이미 다수 수정함).
루트 컨테이너(프레임)에 overflow: hidden 해제/조정해서 내부 content가 잘리는 문제 방지.
완전한 재구성(권장, 시간이 허락될 때)

page1.jsx를 예시로 수동 리팩토링: CSS 모듈(.module.css) 혹은 Tailwind로 재작성 → 나머지 페이지 템플릿을 동일 패턴으로 복제.
제가 해드릴 수 있는 것 (선택)

옵션 1(빠른 데모): 지금 page1.jsx를 “시각적으로 Figma와 최대한 동일” 하도록 깔끔하게 리팩토링하여 예시를 보여드리겠습니다. (수동, 1파일)
옵션 2(전면 자동 정리): 프로젝트의 모든 page*.jsx 파일에서 흔한 문제(예: style 문자열 → 객체, root height, 잘못된 속성) 자동으로 찾아 수정해 드립니다. (빠르지만 완벽하지 않음)
옵션 3(정교한 재구성): page1.jsx를 컴포넌트/모듈 CSS 구조로 완전히 재작성 → 사용 패턴을 제공 → 나머지 페이지에 적용 가능. (시간 소요)
질문(간단)

어느 옵션으로 진행할까요? (1: 예시 리팩토링 page1, 2: 자동 전파일 정리, 3: 완전 재구성)
또는 바로 시작하지 말고, 제가 지금 어떤 샘플 코드를 보여드리길 원하십니까?
다음 단계(제가 바로 시작하면)

선택하신 옵션을 알려주시면, TODO를 업데이트하고(진행중 표시) 바로 작업을 시작하겠습니다.